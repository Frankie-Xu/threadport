# T04 · 事实与人工状态模型

## 本地 Issue 与验收

Task T04 / P0 / F04,F05 / Q05,Q06,Q15。基线 d184e1aef2f8469374b3f364f6b8bf30bc09f288；T03 PR #29 已合并，六组 CI 与 required check 成功。分支 codex/t04-evidence-model。

旧行为把首条用户消息固定为目标，把 assistant 计划收为决策，历史测试缺少当前有效性边界。

- [x] Q05：A→assistant 计划 X→用户修正 B，B 只是 derived 候选，A/X 留作消息证据，决策列表不自动采纳 X。
- [x] Q06：中文禁止句原文及引用保留；人工目标/下一步/约束（含用户清空）优先，重扫不改生命周期或归档。
- [x] Q15：历史 exit 0 保持观察事实，当前工作区有效性明确未知。
- [x] 无事实时目标为 null；空时间不填造；固定输入重复结果一致，不修改输入。
- [x] 四个旧 adapter 和 CLI 接入新派生逻辑；Capsule v1 校验继续通过。

## 验证记录

环境：2026-09-14，macOS arm64，Node 24.18.1 / npm 11.16.0。

基线 npm run check：23 文件 / 180 测试通过。新增 adapter 回归在旧实现上两项失败，分别证明目标修正未生效与历史有效性说明缺失。领域模块初始缺失，随后 12 项领域用例通过。旧 fixture 的五处首消息目标断言按新候选语义更新；同时保留原消息证据断言。

最终 npm run check：25 文件 / 194 测试通过，typecheck/build 通过。新增共 14 项测试覆盖纯函数、引用、人工优先、跨目录/会话失败、未知值及旧适配器投影。npm run check:docs：25 个文档 / 35 个本地目标通过。npm run check:pack：53 个包文件，隔离安装 CLI 与公共导出通过。git diff --check 通过。

## 自审、阶段门和限制

按范围、正确性、数据去向、兼容、可维护性、证据顺序完成 AI self-review；无已知 P0/P1。这不是独立人工审计。没有新增依赖、网络请求、命令执行能力或源日志写入。已有隐私回归通过，消息先脱敏再生成限长证据摘要。

[ADR 0007](../adr/0007-unknown-claim-time.md) 修正内部 Claim 的未知时间契约，不修改公开 schema。候选目标按源顺序取最新用户首行，不能识别其是否完整任务；必须确认。约束提取只是规则，完整原消息由来源证据承载。临时事件 ID 不是 T06 持久身份；bridge 不填造历史命令时间或路径列表。

P0 基础门：既有回归及 Q01–Q06/Q15 对应合成用例通过，T01–T04 基础增量达到 L2。Q06 的实际编辑/持久保存需 T08，Q15 的当前 Git 验证需 T10；未宣称完整 F04、实机接续或 L3 发布认证。resolveTaskState 是供 T08 使用的纯合成接口，当前产品实际使用 deriveTask。

## 交接

保留现有 .gitignore、research 与两个 HTML 实验，不纳入本任务。下一项 T05：本地存储与安全迁移。远端 CI 结果与合并状态以 PR 为准。
