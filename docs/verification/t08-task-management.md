# T08 · 可编辑任务与会话关联

## 本地 Issue 与实施步骤

基线 0d00f6d8f6640a4f6021f8d1bd97e3e07eb8b9ac，T07 PR #33 已合并。T08 / F04 / AC-F04-1–4 / Q06,Q10,Q22。现有 Store 支持底层 CAS，但没有任务用例、字段预算、项目关联检查或完成后活动判断。

1. 新增 src/tasks/contracts.ts：TaskPatch、创建输入、字段长度与脱敏预览；title 1–120、objective 8000、constraint 2000 × 50、nextAction 4000。新增 service.ts：create/update/attach/detach/detail，以既有 Store 为事务端口。
2. tests/tasks/service.test.ts 先验证缺失入口失败，再覆盖创建、人工来源、生命周期、归档撤销、重扫保护、旧 revision 冲突、关联原子性/独占/跨项目、解绑持久化、缺失输入及脱敏拒绝。
3. 扩展 sqlite-store.ts，原子保存任务、修订和关联快照；显式关联未绑定会话时绑定任务项目，已有不同项目拒绝。工作区必须属于同项目；不做路径/Git 自动推断。
4. schema 3 保存完成事件基线和修订关联快照；旧历史无法恢复的关联列表保留 null。完成后新事件只产生 attention，重新完成才更新基线。
5. 执行目标测试、npm run check、npm run check:pack；自审和文档检查后提交、推送 PR，三平台 CI 通过再合并。

不包含 UI、HTTP 层或真实 Agent 认证。REVISION_CONFLICT 后续由 T11 映射 HTTP 409。本地无关改动不纳入提交。

## 验收与验证记录

2026-09-14 / macOS arm64 / Node 24.18.1 / npm 11.16.0。首次目标测试因 tasks/service 模块不存在失败；实现后逐步补充行为回归。

- [x] AC-F04-1：创建、人工目标/约束/下一步、暂停/完成/重开、归档撤销；列表默认隐藏归档。
- [x] AC-F04-2：来源索引没有隐式任务；人工字段经追加/重扫仍保留；绑定、任务、修订、关联在 SQL trigger 故障注入后一起回滚；移出到未归类状态跨连接持久化。
- [x] AC-F04-3：旧 revision 拒绝覆盖第一次编辑；抢占会话返回冲突且不修改任何任务。
- [x] AC-F04-4：detail 分开返回 user-confirmed 字段和 derived 候选；任务/会话/项目缺失与跨项目关联明确报错。同项目两个 worktree 可关联。
- [x] 字段边界最大值通过、超限拒绝；中文约束完整保存；秘密文本要求先预览，历史表无原始秘密。
- [x] 无时间戳的新消息也能触发完成后活动提醒；归档、重启和重复扫描不清除提醒；重开再完成刷新基线。
- [x] v1/v2 → v3 升级、失败回滚和备份恢复通过；已有完成任务从升级时的事件开始跟踪，旧关联历史为 null。

`npm run check`：33 文件 / 241 项测试、typecheck、build、文档检查通过。`npm run check:pack`：88 包文件，隔离安装后的 CLI、storage、sources、indexing、tasks 公开接口加载和创建/编辑任务通过。

## 自审与交接

AI self-review：范围→正确性→数据流→兼容/恢复→维护性→验收证据，无已知未解决 P0/P1，不等同独立审计。新增 schema 3 使用原 SQLite 备份和短事务迁移流程，无新依赖或旧 Capsule/CLI 格式变化。所有来源使用合成数据和临时目录，没有真实日志、网络传输或命令执行。

只提供 SDK 用例；HTTP 409 与可交互脱敏确认由 T11/T15 接入。Store 工作区登记不代表路径验证，T10 负责真实目录身份验证。任务详情读取受全局索引容量约束，本任务未做大规模性能认证。

本任务 L2（任务集成）。提交、远端三平台 CI 和合并证据以本任务 PR 为准；下一项 T09「中英文历史检索与分页」。用法见 [人工任务](../v0.2/09-task-management.md)。

首轮 Ubuntu CI 暴露 T07 取消测试依赖累计 100 条事件的时序假设：按 100 ms 提交可能在末批才跨过阈值，此时任务已读完。测试改为首个非空批次后取消（每批上限 100，小于总量 150），继续要求取消时已存数量在 0 与 150 之间，以及重启恢复后恰好 150 条。生产提交策略未改变。
