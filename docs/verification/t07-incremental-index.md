# T07 · Codex 来源与幂等增量索引

## 本地 Issue 与验收

T07 / P1 / F01,F02 / Q07,Q08,Q09。基线 edb05dd8292df4069df12a596f1b93aebc48ba16，依赖 T06 PR #32 已合并。分支 codex/t07-incremental-index。原生 Codex 注册首次测试因不支持该 agent 失败；此前没有持久增量索引用例。

- [x] Claude/Codex 共用有界读取契约；Codex 合成 fixture 覆盖可见消息与跨页调用结果，排除隐藏 reasoning，未知退出结果不填零。
- [x] 重复扫描不重复、追加两事件恰好新增两条，替换重建不覆盖人工任务字段或关联；源文件内容保持不变。
- [x] 缺失来源留墓碑和最后提交游标；单个失败来源不阻塞其他来源。
- [x] 同源重复请求复用 job，跨实例数据库占用限制两个来源；过期占用回收和旧 owner 拒绝提交。
- [x] 每批最多 100 事件；事件与完整游标同事务，SQL trigger 故障注入证明批次回滚，旧游标提交被拒绝。
- [x] 取消后重启从已提交游标继续，150 事件无重复；15 秒调度和排队取消分别验证。
- [x] schema 1 → 2 保留人工任务、关联、旧游标；迁移故障及备份恢复回归通过。
- [x] 全局 100000 事件边界拒绝新增并回滚会话/游标，保留既有索引；另设 1 GiB 累计游标容量上限。

## 验证记录

2026-09-14 / macOS arm64 / Node 24.18.1 / npm 11.16.0。

`npm run check`：32 文件 / 233 测试全部通过，typecheck、build 与文档链接检查通过。`npm run check:pack`：83 个包文件，隔离安装验证 CLI、SQLite、sources 与 indexing 公开入口。全部输入来自合成 fixture 和临时目录，没有读取真实 HOME 或上传日志。

## 审查与边界

AI self-review，检查范围、正确性、数据流、兼容、失败恢复与验收覆盖；无已知未解决 P0/P1，不等同外部审计。SQLite 迁移增加完整 cursor 和数据库占用表；升级前备份、迁移失败回滚沿用 T05。无旧 Capsule/CLI 格式变化，无新依赖，无源文件写入或命令执行。

[索引用法与恢复](../v0.2/08-indexing.md) 说明提交中间状态为 partial、比较游标拒绝陈旧提交、重建仅替换索引事件。占用过期机制不等同操作系统强制终止暂停的文件读取。来源指纹使用有界摘要，不宣称检测任意中部原地修改。1 GiB 限额度量累计已索引字节，不是数据库磁盘配额。

[Codex 兼容边界](../../compatibility/codex-source.md) 只对应提交的合成格式，不宣称真实 Agent 版本认证。未执行 200 MiB 性能基准、实机接续或 UI 验收；容量性能后续由 T16 验证。

## 交接

本任务 L2（索引集成）。远端三平台 CI、提交及合并状态以本任务 PR 为准；下一项 T08 可编辑任务与会话关联。无关 .gitignore、research 与两个 HTML 实验文件不纳入提交。
