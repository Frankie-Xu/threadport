# T16 · 固定容量与响应预算

| 工作包 | 提交 | 文件 | 测试 | 回滚 |
| --- | --- | --- | --- | --- |
| T16-A 可复现容量证据 | perf(T16): measure bounded indexing and interactive response | scripts/benchmark.mjs、tests/indexing/capacity.test.ts、package.json、docs/verification/performance-beta.md；只按实测瓶颈调整 indexing/search/server | 200 MiB、500 sessions、50k events；100 次中英/缺失/混合查询，索引并行 status、5 次冷启动、增量、RSS/CPU、取消；100k events/1GiB 事务限额 | 单独 squash；无迁移时保留 schema 4；若后续消费者依赖先逆序回滚 |

先保存未经优化的实际测量；不减少固定数据量、跳过中文或缺失查询来满足预算。基准服务在独立子进程，统计其 RSS/CPU，生成器不计入服务内存。临时数据只含合成日志，正常/异常退出负责关闭子进程并清理自己创建的目录。机器差异、未测 UI 响应和失败门禁如实记录；不将共享 CI 毫秒噪声作为性能认证。
