# T15 · 本地工作台实施计划

当前任务内按总计划执行，不等待第二次开发许可。UI 按规格默认英文，中文/Unicode 内容原样支持。设计使用经过产品类检索确认的 Flat Design：系统字体、清楚的层级、可见键盘焦点、44px 操作目标、有限动画及 reduced-motion。检索中的营销首页/外部字体不适合本地工具，不采用。

| 工作包 | 提交 | 文件 | 测试 | 回滚 |
| --- | --- | --- | --- | --- |
| T15-A 配置/收件箱/历史 | feat(T15-A): add the local task workbench | web/index.html、vite.config.ts、tsconfig.json、src/main.tsx、app.tsx、api.ts、styles.css、components.tsx、features/{onboarding,inbox,history,settings}/；src/server/bootstrap.ts、auth.ts、app.ts、business-routes.ts；src/storage/api-store.ts；package.json/lock、tsconfig/scripts/CI；tests/cli-ui.test.ts、tests/e2e/workspace.spec.ts、playwright.config.ts；验证/接口/进度文档 | 首次绑定与显式来源扫描、创建任务、未归类/历史过滤分页、恶意 HTML、刷新认证恢复、1280/1440/768、键盘、production assets/offline/CSP、三平台 CI | 一份 squash；先回滚 B；保留服务/API/数据库人工数据，回到旧入口页 |
| T15-B 编辑/接续预览 | feat(T15-B): edit tasks and review continuations | web/src/features/task/、handoff/ 及 app/api；src/server/business-routes.ts、storage/api-store.ts、tasks/service.ts 的安全详情/证据投影；tests/e2e/task-handoff.spec.ts 与回归/验证文档 | dirty patch、双标签 revision 冲突、关联/移除/生命周期/归档、脱敏确认、证据定位、完整 preview/省略/unknown/漂移、确认仅复制命令、下载完整 bytes | 独立 squash；保留 A 和数据；真实 T14-B gate 不被 UI 测试替代 |

- [ ] A：先添加可失败浏览器/静态资产测试，构建 React/Vite 的纯本地资源，接入真实 API，再验证与提交/推送/PR/CI。
- [ ] B：接编辑与交接流程，按真实接口补详情投影与证据定位，跑全部 E2E/边界测试并完成 GitHub 流程。

约束：不引入账户、遥测、远程字体、内嵌终端或设计系统依赖；token 从 fragment 读入闭包后立即清理，不写 Web Storage；刷新保留查询并允许粘贴当前 CLI 链接恢复。静态资源仅由构建清单/固定资产映射提供，不能让浏览器读取任意服务器文件。错误有可访问反馈，所有异步提交有同步重复提交保护。

测试框架是用户指定开发计划中的 Playwright E2E 文件；手工视觉 QA 仍用 Playwright CLI。所有测试源/任务/能力为明确的合成 fixture，真实 Agent 认证仍留 T14-B/T18。
